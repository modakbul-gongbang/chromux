# Context Notes

- PRD: agents/prd/benchmark-followup/prd.md
- Approval: the user said `gogo` on 2026-07-12 after the pending `ho-build` approval gate was explained.
- Baseline: this worktree is branch `prd/benchmark-followup` from fetched `origin/main@7e2dbf4197a3bf0cbbd1e0975649ad7b7efa4edd`, the PR #13 merge commit named by the handoff.
- Package baseline: `@team-attention/chromux` version `0.18.0`, Node.js `v22.13.1`, no runtime dependency change or release intent.
- Environment: Claude Code `2.1.207` is authenticated, Google Chrome Stable exists at `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`, npm registry access passed, and GitHub CLI is authenticated.
- Official v2 task IDs: `form-order`, `feed-extract`, `nav-tour`, `sequential-steps`, `inventory-aggregate`, `signup-challenge`, `miniwob-email-inbox`, `miniwob-book-flight`, `hn-top-story`, `wikipedia-hop`, `google-search`, `youtube-search`.
- Post-v2 task IDs: `shop-cookie-select`, `slow-order`, `iframe-register`, `miniwob-use-autocomplete`, `miniwob-login-user`, `miniwob-search-engine`, `miniwob-click-checkboxes`, `wikipedia-extract`.
- Source of truth: `buildTasks()` in `benchmarks/agent-compare-benchmark.mjs` currently defines exactly those 20 task IDs.
- Sonnet budget: historical same-rep chromux plus playwright-cli cost was `$9.16` for 40 sessions, so the approved rerun will use `--reps-local 2 --reps-external 1` and is expected to remain near that amount.
- Opus budget: defaults would create 165 sessions and extrapolate to about `$42.42`, above the PRD's `$40` hard stop.
- Opus run decision: use `--reps-local 2 --reps-external 1`, which creates 105 sessions and extrapolates to about `$27.00` from the prior 96-session `$24.68` combined run.
- Verification deviation rationale: the PRD benchmark command examples omit explicit reps, while the handoff explicitly requires adjusting reps to stay near `$30`; adding the two reps flags preserves all tasks, all tools, machine grading, and same-run comparison while honoring the budget cap.
- Execution order: despite the generated graph's no-dependency warning, the coordinator will execute N1, N2, N3, N4, N5, N6, then N7 because each documentation node depends semantically on its preceding report.
- Existing documentation issue: `install.md` still claims `.github/workflows/npm-publish.yml` publishes on `main`, contradicting the current repository guide and the absent workflow; resolve this narrow stale guidance under N6 without changing release behavior.
- Sonnet outcome: the approved 12-task chromux/playwright-cli rerun completed 40 sessions, passed 39/40, and cost `$5.15`; chromux passed 20/20 and the sole playwright-cli failure was a live Hacker News title/API mismatch.
- Opus outcome: the approved expanded run completed 105 same-run sessions, passed 102/105, and cost `$18.45`, below the `$30` target and `$40` hard stop.
- Expanded aggregate: chromux passed 35/35 in 18.2 minutes, 245 turns, 4.24M tokens, and `$4.72`; agent-browser passed 33/35 in 20.3 minutes, 254 turns, 5.21M tokens, and `$8.08`; playwright-cli passed 34/35 in 26.4 minutes, 292 turns, 5.34M tokens, and `$5.65`.
- Expanded failure audit: agent-browser failed Hacker News because the visible title was absent from grading API samples and failed Google with no qualifying URL; playwright-cli failed Google with no qualifying URL after 439.5 seconds.
- Expanded chromux trace audit: the highest-turn sessions were finite MiniWoB UI flows; an email text-selector miss recovered through an interactive snapshot, and an inventory regex escaping error recovered on the next command.
- Runtime tracker limitation: the Codex Goal remained in its prior `blocked` status after the user resumed with `go ahead`; `create_goal` rejected a replacement because the unfinished goal already exists, so the harness state remains the completion authority.
- Intentionally unchanged surfaces: `skills/chromux/SKILL.md` and `skills/chromux-work/SKILL.md` remain unchanged because this work changes benchmark evidence and documentation, not CLI syntax, response shapes, setup, or browser-work guidance.
- Token Footprint decision: the README Token Footprint table remains unchanged because no `snapshot`, `open`, response-shape, or runtime payload code changed.
- Version decision: `package.json` remains at `0.18.0` because the PRD has no package-release intent and explicitly disallows a version bump without separate approval.
- Harness decision: `benchmarks/agent-compare-benchmark.mjs` remains unchanged because its current `buildTasks`, task filters, tool filters, and repetition flags already support both approved runs.
- Validation outcome: V1 help, V2 doc check, V5 benchmark smoke, V6 real Chrome suite with 222 passed and 0 failed, and V7 package dry-run all pass; the tarball excludes local `agents/implement` evidence.
